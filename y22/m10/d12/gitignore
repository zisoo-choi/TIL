1. 반복문으로 10000까지 돌리고, temp (1) = i (1) 를 집어넣는다.

2. 또 다른 반복문으로 temp (1) < 10000 이면 반복한다.

3. 조건문으로 arr[i]이 == 0 이면 조건문을 실행한다. (-> arr[1] == 0)

4. arr[self(temp)]로
ㄴ 1 : self(temp) => self(1) 함수를 실행 -> return  값으로 2를 받음
ㄴ 2 : arr[2]에 1을 넣어줌 -> arr[2] = 1

5. 조건문을 나가서 temp = self(temp) 해줌 -> self(2)이니까 temp=2가 된다.

6. 다시 while 반복문으로 10000까지 반복한다. 이렇게 반복하면 생성자가 있는 수에는 현재 배열 값에 1이 들어가 있다.

7. 생성자가 있는 수를 배열에 1을 넣어줬으니 출력을 하려면 arr[i]가 0인 배열이 셀프넘버가 된다.